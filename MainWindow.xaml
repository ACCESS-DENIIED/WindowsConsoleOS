<Window x:Class="WindowSelector.MainWindow"
        Activated="Window_Activated"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WindowSelector"
        Title="MainWindow" Height="350" Width="600"
        AllowsTransparency="True"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        WindowStyle="None"
        Background="Transparent"
        WindowState="Maximized">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#FF6200EE"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#FF03DAC6"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#FF121212"/>
        <SolidColorBrush x:Key="OnBackgroundColor" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#FF333333"/>
        <SolidColorBrush x:Key="OnSurfaceColor" Color="#DEFFFFFF"/>
        <local:LazyImageLoader x:Key="LazyImageLoader"/>
        <local:StringToUpperConverter x:Key="StringToUpperConverter"/>
        <Storyboard x:Key="OpenAudioDevicePopupAnimation">
            <DoubleAnimation Duration="0:0:0.3" From="300" To="0" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"/>
            <DoubleAnimation Duration="0:0:0.3" To="1" Storyboard.TargetName="PopupContent" Storyboard.TargetProperty="Opacity"/>
        </Storyboard>
        <Storyboard x:Key="CloseAudioDevicePopupAnimation">
            <DoubleAnimation Duration="0:0:0.3" From="0" To="300" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"/>
            <DoubleAnimation Duration="0:0:0.3" To="0" Storyboard.TargetName="PopupContent" Storyboard.TargetProperty="Opacity"/>
        </Storyboard>
    </Window.Resources>

    <DockPanel LastChildFill="True">
        <!-- Main Content -->
        <ui:SimpleStackPanel x:Name="MainContent" DockPanel.Dock="Top" Margin="12" Spacing="24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ListBox VirtualizingStackPanel.IsVirtualizing="True"
         VirtualizingStackPanel.VirtualizationMode="Recycling"
         Background="Transparent" 
         BorderThickness="0" 
         x:Name="WindowListBox" 
         HorizontalAlignment="Stretch" 
         VerticalAlignment="Stretch" 
         Margin="10" 
         SelectionChanged="WindowListBox_SelectionChanged" 
         Grid.Column="0">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource OnBackgroundColor}"/>
                        <Setter Property="Padding" Value="8"/>
                        <Setter Property="Margin" Value="4"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource SurfaceColor}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Background="Transparent" Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Width="40" Height="40" Source="{Binding Icon}" Grid.Column="0"/>
                            <TextBlock Text="{Binding Name}" FontSize="72" Foreground="White" Margin="10,0" VerticalAlignment="Center" Grid.Column="1"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Enhanced Preview Image with Rounded Edges and Shadow -->
            <Border Background="Transparent" Grid.Column="1" Margin="10" CornerRadius="1" VerticalAlignment="Top" HorizontalAlignment="Right" Padding="5">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="320" ShadowDepth="5" BlurRadius="10" Opacity="0.5"/>
                </Border.Effect>
                <Image x:Name="PreviewImage" Source="{Binding SelectedItem.WindowHandle, ElementName=WindowListBox, Converter={StaticResource LazyImageLoader}}" Stretch="Uniform"/>
            </Border>
        </Grid>

        <!-- Popup for Audio Devices -->
            <Popup x:Name="AudioDevicesPopup" Placement="Center" AllowsTransparency="True" IsOpen="False">
                <Border x:Name="PopupContent" Background="Black" HorizontalAlignment="Right" Margin="20" CornerRadius="10" 
                        BorderBrush="#FFFFFF" BorderThickness="2">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="2" Opacity="0.5"/>
                    </Border.Effect>
                    <Border.RenderTransform>
                        <TranslateTransform x:Name="popupTranslateTransform" X="300"/>
                    </Border.RenderTransform>
                    <StackPanel>
                        <TextBlock Text="Select Audio Device" Foreground="#FFFFFF" FontSize="16" Margin="10,5" HorizontalAlignment="Center"/>
                        <StackPanel x:Name="ContentPanel" Visibility="Visible">
                            <TextBlock x:Name="LoadingTextBlock" Text="Populating List..." Foreground="White" HorizontalAlignment="Center" Visibility="Collapsed"/>
                            <ListBox x:Name="AudioDeviceList" Background="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="10">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="#FFFFFF"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="DodgerBlue"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding FullName, Converter={StaticResource StringToUpperConverter}}" Foreground="#FFFFFF"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Popup>
        </ui:SimpleStackPanel>
        <Canvas HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="400" Height="100" Margin="10">
            <Image Source="Legend.png" Width="600" Canvas.Right="0" Canvas.Bottom="120"/>
        </Canvas>
    </DockPanel>
</Window>